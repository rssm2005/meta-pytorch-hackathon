[project]
name = "meta-pytorch-hackathon"
version = "0.1.0"
description = "Hackathon submission"
authors = [{name = "Pranav"}]

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
